export type CountryDetail = {
  capital: string
  code: string
  continent: {
    name: string
  }
  currency: string
  emoji: string
  languages: {
    name: string
  }[]
  name: string
  native: string
  phone: string
}
